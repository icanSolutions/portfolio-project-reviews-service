version: '3.8'


networks:
  backend_network:
    external: true

services:
  products:
    build: .
    container_name: reviews_service
    env_file:
      - .env.prod
    ports:
      - "5002:5000"
    volumes:
      - .:/app
    networks:
      - backend_network
    