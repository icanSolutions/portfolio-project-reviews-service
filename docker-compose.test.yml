version: '3.8'


networks:
  backend_network:
    external: true

services:
  reviews:
    build: .
    container_name: reviews_service
    env_file:
      - .env.test
    ports:
      - "5002:5000"
    volumes:
      - .:/app
    networks:
      - backend_network
    